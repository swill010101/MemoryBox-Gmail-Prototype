# MBAT-P2-I11 — Narrative & Summaries

**Increment:** P2-I11 · **LOCKED** 2026-08-24 (founder narration-prep) · prep/LLM prove items apply **when build-authorized**  
**Prove:** `python -m memorybox prove-i11`

| ID | Criterion |
|---|---|
| C-01 | `Tell me about` / `what do you know` / summarize / what happened / what was X like → `output_mode=tell`. Semantic family, not a phrase table. |
| C-02 | `Show me Peggy` → `output_mode=show`; curator is a result set, not an essay. |
| C-03 | `tell` pack includes Journal / Story / retrieved comms even when gallery hides Email/SMS. |
| C-04 | Curator for `tell` uses prepared/synthesized text, not rebuilt only from visible tiles. |
| C-05 | Shared long-form curator on Explore **and** Person Explorer; Copy + Save as Story; no `/narration/ui`. |
| C-06 | Copy creates no Story / Journal / Ask rows. |
| C-07 | Save as Story creates a **working draft** (`ask_available` false) until owner Save Story; `composed_by_model` / equivalent. |
| C-08 | Persistable JSON: original Ask + `output_mode` + `plan` + `presentation` (I13 storage later). No Save View control. |
| C-09 | `said about` / `what did X say` stays communication-focus, not a life essay, unless the Ask is clearly tell. |
| C-10 | No family-nav Narration destination. |
| C-11 | Spam/Trash never in the model pack (Ask eligibility). Originals not destroyed. |
| C-12 | Email units are authored-text, not quoted dumps, when deterministic cleanup is confident. |
| C-13 | Peggy-and-I discussion Asks filter authorship; group threads stay labeled group. |
| C-14 | Calendar selection follows Ask breadth (narrow vs year). |
| C-15 | `tell` LLM (when authorized) is I7A-traced; pack in assembled context; model does not elect trust/spam. |
| C-16 | Broad Asks do not send every retrieved row; units keep provenance. |

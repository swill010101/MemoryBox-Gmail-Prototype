MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1m
run_id: 20260831T0845Z-cloud-c1m
code_commit: 62c4afbeeeef40a5077fe7005b36a41464c88064
stage: preparation-only
scope: tokenized-scalar-in-reply-to
status: READY_FOR_REVIEW

## Lightweight PRD (C1m)

Problem (C1k advisory on `2344fa47`): neighbor SQL compared the entire `in_reply_to` scalar to one wanted ID. A header with several Message-IDs or comment text missed an available child; that child never produced an unresolved marker, so completeness could stay true.

Success: scalar In-Reply-To is split and each token is canonicalized the same way as References; a synthetic multi-ID header attaches the child.

In: neighbor SQL + `_rfc_ids` already-tokenizing extractor + synthetic proves.
Out: FlightSim prepare, Gemma/Sol, chunking, merge. Does not overwrite C1l/C1k reports.

This branch also contains C1l (membership thanks + recomputed replay budget) at `45be496`; this report is only the C1k In-Reply-To finding.

## What changed (code at bound SHA)

Scalar `in_reply_to` match is `regexp_split_to_array(..., E'[\\s,;]+')` plus `_rfc_canon_sql(irt_tok)`, not whole-string equality. Param count unchanged (still four `ANY(%s)` arrays).

## Tests actually run

`MEMORYBOX_ALLOW_DEV_DEFAULTS=1 python3 -m memorybox prove-trusted-identity-retrieval` → `problems: []` at `62c4afbeeeef40a5077fe7005b36a41464c88064`.

New: `review_rfc_ids_extracts_each_id_from_multi_in_reply_to`, `review_neighbor_scalar_in_reply_to_with_multiple_ids_attaches_child`. SQL has ≥2 `regexp_split_to_array`. Live Postgres execute: 0 rows, no error.

Not run: FlightSim prepare. No Gemma.

## FlightSim / human / model

- Year-fair Gemma+Sol FEV2: **NO** (`3cf95fa4` is not Phase 2)
- Semantic chunking: **NO**
- Inference: **NONE**

```
cd C:\memorybox
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
python -m memorybox prepare-trusted-email-review --person "Peggy George" --flightsim
```

Keep prior REVIEW_*. Do not git-add. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

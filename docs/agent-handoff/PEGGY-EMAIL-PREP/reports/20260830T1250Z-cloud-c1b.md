MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1b
run_id: 20260830T1250Z-cloud-c1b
code_commit: f7765d0b96d33666c7ed24fe05d692cae5d6ba24
stage: preparation-only
status: READY_FOR_REVIEW

## Why C1b (does not overwrite C1)

C1 (`a30229ff95f43315e5b3986d3e769cea2df0dd15`, report `20260830T1235Z-cloud-c1`) is kept as a checkpoint. After pull, the full local prove suite failed two existing review checks, quote speaker/date fields were not retained, weak-keyword mail would drop from the pack, and origin had re-enabled automatic Phase 3 on the old gate. This pass finishes authorized C1 items. It is not a new task and does not authorize inference.

## What changed (C1b)

1. **Lossless bodies.** Unchanged path: `segment_review_body` has no 8000/4000 cut. Added a check that default `authored_email_text` still caps while review prep keeps the unique tail.
2. **Quotes.** Forwarded/original delimiters, From/Sent header blocks, nested `On … wrote:`, `>` inline quotes, and HTML-recovered forwards now keep unique text. Available From/Date/Sent fields are retained with provenance and uncertainty. Short unique quotes stay. Dedup is exact or long contained copy only; uncertain overlap is kept.
3. **Budget (no inference).** Existing refuse-unknown path kept. Added mocked plans: serializes reviewed `num_ctx`; refuses advertised-only without matching env; refuses oversize without truncate/refreeze/cloud fallback.
4. **Threading.** Message-ID-only remains singleton. Missing In-Reply-To remains `missing_parent`. Shared subject without a reply edge remains uncertain (extra check).
5. **Authorship.** Service-only packets excluded with `service_only_no_personal_contribution`. Weak keyword alone is `unresolved`, not service, and stays in the pack when From is trusted. Personal greeting beside a strong service notice stays `personal_plus_service`. System prompt still treats source as evidence, not instructions.
6. **Gate.** Automatic `-Step Chunks` removed again. Phase 3 is not authorized. Prepare still is not invoked by the old gate.

Prior C1 files remain. This pass also edits `tools/flightsim-trusted-identity-gate.cmd`.

## Tests actually run

Host: Cursor cloud agent, not FlightSim. Synthetic unit checks only. No archive bodies. No live Gemma/Sol.

Results: `problems: []`. Review checks 26/26 PASS (listed in prove output). Full `prove-trusted-identity-retrieval` unit envelope had no problems.

Not run: `prepare-trusted-email-review --flightsim`. No model call.

## FlightSim / human / model

- Code-complete (C1b unit checks): **YES**
- FlightSim-prepared: **NO** (regenerate local `REVIEW_*` after pull; gitignored)
- Human-approved hash: **NO**
- Inference: **NONE**
- Model-evaluated: **NO**

Phase 1 prior report (5716 / unsupported 0 / 699 candidates) is unchanged and not re-verified here.

## How to regenerate the local packet (FlightSim)

```
cd C:\memorybox
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git checkout cursor/p2-i11a-trusted-identity-retrieve-49da
git pull origin cursor/p2-i11a-trusted-identity-retrieve-49da
tools\flightsim-prepare-trusted-email-review.cmd
```

Inspect MODEL_PASTE locally. Do not git-add. Do not run the old trusted-identity gate. Do not run Gemma until Tom names the hash.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1n
run_id: 20260831T0850Z-cloud-c1n
code_commit: 1e6dddc2ef526a3c42c85785169d7df024594616
stage: preparation-only
scope: conservative-replay-fit-and-membership-clause-keep
status: READY_FOR_REVIEW

## Lightweight PRD (C1n)

Problem (C1l advisory on `45be496`): (1) Replay used only the recomputed prompt, so a bound sidecar count of 500k could pass. (2) Usable room subtracted the module gen constant, not hash-bound `num_predict`. (3) Membership thanks classified the whole message as service, dropping “See you Friday.” The C1k scalar In-Reply-To item was already fixed in C1m on this branch (`62c4afb`).

Success: oversize uses the larger of sidecar vs recomputed; usable = num_ctx − bound num_predict − safety; membership-thanks sentences drop while other sentences stay.

In: `plan_gemma_replay`, `replay_usable_input_tokens`, membership remainder helper, synthetic proves.
Out: FlightSim, Gemma/Sol, chunking, merge. Does not overwrite C1m/C1l reports.

## What changed (code at bound SHA)

1. `prompt_for_fit = max(sidecar_prompt, recomputed_prompt)`.
2. `replay_usable_input_tokens(num_ctx, num_predict=binding.num_predict)`.
3. `_personal_remainder_after_membership_thanks` keeps “See you Friday.”
4. C1m tokenized `in_reply_to` remains in this tree.

## Tests actually run

`MEMORYBOX_ALLOW_DEV_DEFAULTS=1 python3 -m memorybox prove-trusted-identity-retrieval` → `problems: []` at `1e6dddc2ef526a3c42c85785169d7df024594616`.

Includes membership “See you Friday”, larger-of sidecar/recomputed oversize, bound `num_predict` usable (32768−2048−2048), C1m multi-ID In-Reply-To.

Not run: FlightSim prepare. No Gemma.

## FlightSim / human / model

Year-fair Gemma+Sol FEV2: **NO**. Chunking: **NO**. Inference: **NONE**.

```
cd C:\memorybox
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
python -m memorybox prepare-trusted-email-review --person "Peggy George" --flightsim
```

Keep prior REVIEW_*. Do not git-add. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

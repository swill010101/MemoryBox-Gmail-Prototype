MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1g
run_id: 20260830T1645Z-cloud-c1g
code_commit: 623260c648b8e8ff306fe07b90f4df0e048a25ed
stage: preparation-only
scope: final-general-sanitation-and-measurement
status: READY_FOR_REVIEW

## Lightweight PRD (C1g)

Problem: line-based sanitation still let residual promo become trusted-From speech, deleted mixed personal/service lines wholesale, dropped deep forwards silently, leaked generic `[image]` placeholders, and did not report before/after size.

Success: block-aware recursive sanitation; service blocks collapse to generic markers; personal wrappers/clauses/IDs survive; generic images gone; real attachments marked by filename/type only; depth fallback keeps unique text with uncertainty; SOURCE_MAP/PREPARATION_REPORT measure bytes/tokens/counts/reasons; synthetic proves pass.

In: review-packet sanitation, image markers, depth fallback, measurement, wrapper refuse-dirty.
Out: chunking, Gemma/Sol, merge, identity widening, five-year regrouping, brand/sender/date-specific production rules, private paste upload.

## Why C1g (does not overwrite C1–C1f)

Authorized bounded correction after C1e/C1f advisories and Tom's local observation (Hallmark-shaped service still in paste; Threadless-like newsletter gone; generic `[image]` appearing; human content useful). Prior reports stay.

## What changed (C1g)

1. **Block/segment-aware sanitation** on original, quoted, and forwarded bodies. Service/template blocks collapse to `[service notice: a named sender sent a greeting card; unseen contents unavailable]` or a promotional-omit marker. Residual unrecognized promo after recognized template lines is not personal speech.
2. **Mixed lines.** `I picked this for you — view your e-card` keeps the personal clause.
3. **Images.** Generic `[image]` / cid placeholders are removed. Real attachments become `[attached image: filename/type]`. System prompt forbids treating generic images as life evidence.
4. **Depth.** Nested/chained forwards beyond six keep remaining unique text with `nested_forward_depth_uncertain` / `depth_fallback` records. No silent empty drop.
5. **Measurement.** SOURCE_MAP `sanitation_measurement` and PREPARATION_REPORT list bytes/tokens before vs after, retained conversations/messages/turns/IDs, collapse/exclude reasons, generic images removed, attachments retained, depth fallbacks, and `human_evidence_ids_lost` (required empty).
6. **Wrapper.** Refuses dirty tree, in-progress merge, or in-progress rebase without modifying or aborting them. A failed pull is reported without `rebase --abort`.

No Hallmark / Threadless / Peggy / sender / date production rules. Five-year grouping unchanged. No chunking. No models.

## Tests actually run

Host: Cursor cloud agent, not FlightSim. Synthetic only. No archive bodies.

`MEMORYBOX_ALLOW_DEV_DEFAULTS=1 python3 -m memorybox prove-trusted-identity-retrieval`

Results: `problems: []`. Review checks 45/45, including residual promo, mixed-line salvage, generic vs real image, deep-nest fallback, and existing Hallmark-shaped / newsletter / first-person card-mention cases.

Not run: `prepare-trusted-email-review --flightsim`.

## FlightSim / human / model

- Code-complete (C1g unit checks): **YES**
- FlightSim-prepared: **NO** (new `REVIEW_*` after pull; keep prior folders)
- Human-approved hash: **NO**
- Inference: **NONE**
- Chunk experiment: **NOT STARTED**

## How to regenerate (FlightSim)

Refuse dirty/merge/rebase without changing them:

```
cd C:\memorybox
git rev-parse --abbrev-ref HEAD
git status
```

If `git status` is not clean, or MERGE_HEAD / rebase is in progress: **stop and inspect**. Do not commit, abort, or prepare.

Only if the tree is clean and on `cursor/p2-i11a-trusted-identity-retrieve-49da`:

```
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
git rev-parse HEAD
tools\flightsim-prepare-trusted-email-review.cmd
```

Inspect the **new** MODEL_PASTE and PREPARATION_REPORT sanitation_* lines. Keep prior REVIEW_* folders. Do not git-add. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

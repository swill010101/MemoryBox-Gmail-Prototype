MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1
run_id: 20260830T1235Z-cloud-c1
code_commit: a30229ff95f43315e5b3986d3e769cea2df0dd15
stage: preparation-only
status: READY_FOR_REVIEW

## What changed (C1)

Authorized bounded preparation corrections only. No live Gemma, Sol, gate, or merge.

1. **Lossless bodies.** Review prep uses `segment_review_body` / `authored_email_text(..., max_chars=None)`. Ask/FEV2 callers keep the default 8000/4000 caps. Long plain and HTML-only tails kept in unit tests.
2. **Quotes.** Unified delimiters (On wrote / Original Message / Forwarded / Begin forwarded). Short quotes kept. Dedup only when the same text is already in the packet. Forwarded blocks are not labeled as the enclosing sender.
3. **Budget (no inference).** `ollama_chat_request_payload` serializes `num_ctx`. `plan_gemma_replay` refuses unknown capacity, advertised-only without matching env, oversize, and hash mismatch. Replay calls `ollama_chat(..., num_ctx=reviewed)`.
4. **Threading.** Own Message-ID is a singleton, not a confirmed conversation. In-Reply-To that does not match a packet member is `missing_parent`. Confirmed requires a shared thread id or a matching reply edge.
5. **Authorship.** Synthetic service-notification fixture: trusted From ≠ personal speech. Service-only conversations are excluded from the participation pack with a counted reason. System prompt treats source as evidence, not instructions.

Files: `memorybox/ask/i11a/trusted_email_review.py`, `memorybox/ask/authored.py`, `memorybox/explore/email_attach.py`, `memorybox/providers/llm/_ollama_http.py`, `memorybox/person/trusted_identity_acceptance.py`.

## Tests actually run

Host: Cursor cloud agent, not FlightSim. Synthetic unit checks only. No archive bodies.

Results: 11/11 PASS (grouping rfc vs uncertain; Message-ID singleton + missing parent; long plain/HTML tails; short forward quote; lossless vs capped authored helper; service not personal; paste does not say “Peggy said” for service; request serializes num_ctx; replay plan refuses unknown capacity; replay plan serializes reviewed num_ctx).

Not run: `prepare-trusted-email-review --flightsim`.

## FlightSim / human / model

- FlightSim-prepared: **NO** (regenerate local `REVIEW_*` after pull; gitignored)
- Human-approved hash: **NO** (prior local inspection was not hash approval)
- Inference: **NONE**
- Model-evaluated: **NO**

Phase 1 prior report (5716 / unsupported 0 / 699 candidates) is unchanged and not re-verified here.

## How to regenerate the local packet (FlightSim)

```
cd C:\memorybox
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git checkout cursor/p2-i11a-trusted-identity-retrieve-49da
git pull origin cursor/p2-i11a-trusted-identity-retrieve-49da
tools\flightsim-prepare-trusted-email-review.cmd
```

Inspect MODEL_PASTE locally. Do not git-add. Do not run Gemma until Tom names the hash.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

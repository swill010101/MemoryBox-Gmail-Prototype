MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1k
run_id: 20260831T0835Z-cloud-c1k
code_commit: 2344fa47e5e93335e369c1608e8c5281ed889322
stage: preparation-only
scope: rfc-canonicalization-and-cap-frontier
status: READY_FOR_REVIEW

## Lightweight PRD (C1k)

Problem (C1j advisory): `_norm_rfc` brackets a bare `@` ID, but `_rfc_ids` only kept `<...>` tokens, so a bare In-Reply-To never entered the frontier or unresolved list. SQL lowercased stored headers without adding brackets, so a stored bare Message-ID missed a bracketed query key. Attach-cap left `newly_discovered` off `known_rfc`, so child IDs from the capped page were omitted from `unresolved_rfc_ids`.

Success: bare and bracketed IDs are the same canonical key in extraction and SQL; cap incompleteness lists newly attached rows' unsearched RFC IDs.

In: `_rfc_ids`, neighbor SQL canon, fetch `known_rfc` merge on cap, synthetic proves.
Out: thank-you template attribution, bound replay budget (deferred, not waived), chunking, Gemma/Sol, merge, FlightSim prepare (Tom).

Does not overwrite C1j or C1i reports.

## What changed (code at bound SHA)

1. **`_rfc_ids`** keeps `<id@host>` matches and remaining whitespace tokens that contain `@`, stored as `<token>`.
2. **SQL canon** wraps stored Message-ID / In-Reply-To / array / References tokens: if not already `<...>`, add brackets when `@` is present, then `lower(...)`.
3. **Cap frontier** always `known_rfc.update(newly_discovered)` before treating the hop as finished or incomplete, so attach-cap `unresolved_rfc_ids` includes child IDs from the capped page.

Deferred before Gemma (not waived): service-template thank-you attribution; immutable/recomputed replay budget oversize check.

## Tests actually run

Host: Cursor cloud agent, not FlightSim. Synthetic plus live SQL parse.

`MEMORYBOX_ALLOW_DEV_DEFAULTS=1 python3 -m memorybox prove-trusted-identity-retrieval` → `problems: []` at `2344fa47e5e93335e369c1608e8c5281ed889322`.

New proves: bare `_rfc_ids`, unresolved bare In-Reply-To, bare stored parent attach, attach-cap lists `<mN@x.test>` child IDs.

Live: neighbor SQL `.format(id_clause="")` executed against local Postgres; 0 rows, no error.

Not run: FlightSim prepare. No Gemma. No Sol. No chunks.

## FlightSim / human / model

- Code-complete (C1k unit checks): **YES**
- FlightSim-prepared after C1k: **NO**
- Human-approved hash: **NO**
- Year-fair Gemma+Sol FEV2: **NO** (starved `3cf95fa4` is not Phase 2)
- Semantic chunking: **NO**
- Inference: **NONE**

## How to regenerate (FlightSim)

```
cd C:\memorybox
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
git rev-parse HEAD

$env:MEMORYBOX_P1_RUNTIME_HOST = "1"
Remove-Item Env:MEMORYBOX_ALLOW_DEV_DEFAULTS -ErrorAction SilentlyContinue
python -m memorybox prepare-trusted-email-review --person "Peggy George" --flightsim
```

Inspect `neighbor_unresolved_rfc_ids` for missing parents and cap frontiers. Keep prior `REVIEW_*`. Do not git-add paste. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

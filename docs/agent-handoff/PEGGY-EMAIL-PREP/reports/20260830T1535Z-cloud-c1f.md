MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1f
run_id: 20260830T1535Z-cloud-c1f
code_commit: a3a258259cda6426deec693dd5bf1e1663a95e83
stage: preparation-only
status: READY_FOR_REVIEW

## Lightweight PRD (C1f)

Problem: evidence packets still carried promotional HTML, tracking links, navigation, unsubscribe/legal boilerplate, and duplicated quoted service notices inside original, quoted, and forwarded segments. Classify ran on the unsanitized lead, so a personal wrapper beside a template was treated as service-generated. Five-year grouping cannot fix payload contamination.

Success: recursive sanitation on original / quoted / forwarded content; wrappers and useful human conversation remain; `[email_N]`, attribution, dates, and uncertainty labels remain; synthetic Hallmark-shaped thread and human-forwarded newsletter proves pass; five-year interval logic unchanged.

In: review-packet sanitation and regression proves.
Out: five-year window expansion, I11B, historian benchmark, Phase 3, Gemma/Sol, retrieve/Gallery identity changes, archive deletes, Hallmark-word blacklist.

## Why C1f (does not overwrite C1–C1e)

Tom asked to fix sanitation before expanding to five-year grouping. Prior reports stay as checkpoints.

## What changed (C1f)

1. **Recursive sanitize.** `sanitize_review_tree` walks lead, quoted, and forwarded segments (depth 6). Promotional leftover HTML, tracking URLs, navigation lines, unsubscribe/legal boilerplate, and service-notice lines are stripped on every segment.
2. **Classify after clean.** A wrapper such as `Thinking of you this week.` beside a view-your-e-card template is personal. An empty lead whose raw text was a service template stays `service_generated`.
3. **Quoted / forwarded chrome.** Entire newsletter or service quoted blocks omit. Mixed personal-plus-footer quotes still keep the personal portion. Duplicate wrappers already in the packet omit as duplicates, not as a brand delete.
4. **Preserve.** Personal wrappers, useful human conversation, quote attribution, dates, uncertainty labels, and original `[email_N]` evidence IDs stay.
5. **Five-year grouping.** `propose_five_year_interval` is unchanged. It is a later retrieval-window control, not a sanitation substitute.
6. **No Hallmark blacklist.** `I mailed a Hallmark card Friday after the party.` still stays.

## Tests actually run

Host: Cursor cloud agent, not FlightSim. Synthetic unit checks only. No archive bodies. No live Gemma/Sol. No real tracking URLs or correspondence.

Command: `MEMORYBOX_ALLOW_DEV_DEFAULTS=1 python3 -m memorybox prove-trusted-identity-retrieval`

Results: `problems: []`. Review checks 40/40 PASS, including:

- `review_hallmark_thread_keeps_wrapper_strips_template_and_dupes`
- `review_forwarded_newsletter_keeps_fyi_strips_chrome`
- `review_five_year_grouping_is_not_sanitation`
- `review_personal_card_mention_is_not_service_exclusion`

Not run: `prepare-trusted-email-review --flightsim`.

## FlightSim / human / model

- Code-complete (C1f unit checks): **YES**
- FlightSim-prepared: **NO** (new `REVIEW_*` after pull; keep prior failed artifacts)
- Human-approved hash: **NO**
- Inference: **NONE**
- Model-evaluated: **NO**

## How to regenerate (FlightSim)

If git is waiting in an editor: `Esc`, `:cq`, Enter. If merge or rebase is already in progress, **stop and inspect** — do not commit or abort blindly.

```
cd C:\memorybox
git rev-parse --abbrev-ref HEAD
git status
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git checkout cursor/p2-i11a-trusted-identity-retrieve-49da
set GIT_MERGE_AUTOEDIT=no
set GIT_EDITOR=true
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
git rev-parse HEAD
tools\flightsim-prepare-trusted-email-review.cmd
```

Expect `a3a258259cda6426deec693dd5bf1e1663a95e83` or the following report commit. Inspect the **new** MODEL_PASTE. Keep prior REVIEW_* folders. Do not git-add. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

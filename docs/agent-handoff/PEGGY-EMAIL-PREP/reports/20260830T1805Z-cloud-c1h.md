MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1h
run_id: 20260830T1805Z-cloud-c1h
code_commit: 29758f8f375a403ddbcfc8ae3c79f7da08db1354
stage: preparation-only
scope: residual-first-person-promo-targeted-neighbors-bound-replay-honest-measurement
status: READY_FOR_REVIEW

## Lightweight PRD (C1h)

Problem: C1g left four holes. First-person commercial leftovers became trusted-From speech because I/we short-circuited residual promo. Neighbor fetch scanned an unordered LIMIT 8000 and prepare swallowed failures. Replay trusted mutable SOURCE_MAP request settings after the paste hash check. Measurement compared raw bodies to the rendered paste, used mixed token units, and scanned only retained first-person pronouns — so excluded “Love you” / “Happy birthday” could be reported as no loss.

Success: first-person promo leftover is `service_generated`; personal wrappers stay personal; neighbor SQL is targeted and prepare fail-closes; paste hash covers replay settings; measurement compares bodies in one unit, labels paste tokens separately, and scans excluded IDs for independent human speech.

In: the four authorized C1h corrections and synthetic proves.
Out: chunking, Gemma/Sol, merge, identity widening, five-year regrouping, brand/sender/date-specific production rules, private paste upload.

## Why C1h (does not overwrite C1–C1g)

Tom approved the post-C1g advisory (`approved.`). That is GO for these four items only. Not hash approval. Not Gemma. Prior reports stay.

## What changed (C1h)

1. **Residual first-person promo.** I/we/my/our no longer prove speech. Institutional “we’re excited / our members / seasonal collection” after service context is residual promo. `I mailed a Hallmark card Friday after the party.` and `I sent you an e-card because I miss you.` stay personal. No Hallmark-word blacklist.
2. **Targeted, fail-closed neighbors.** `fetch_rfc_neighbor_rows` uses `ANY(%s)` on RFC ids / in-reply-to / references. Saturating the 500-row cap raises. Prepare returns `rfc_neighbor_fetch_failed` instead of continuing with no neighbors.
3. **Replay binding.** Prepare writes `===== REPLAY BINDING =====` with canonical JSON (`model`, `provider`, `num_ctx`, `num_predict`, `temperature`, `format`, `capacity_certainty`) between system and user. The paste hash covers those settings. Replay refuses `replay_binding_missing` or `source_map_replay_binding_mismatch`, strips the binding from the Gemma system/user, and uses paste-bound settings only.
4. **Honest measurement.** Before/after are recovered vs sanitized bodies, both `bytes/4`. Paste tokens are a separate field. Human-loss scans every `need_id` including excluded, using wrappers such as Love you / Happy birthday / I mailed — not bare I/we. Empty loss is not claimed as proof.

No Hallmark / Threadless / Peggy / sender / date production rules. Five-year grouping unchanged. No chunking. No models.

## Tests actually run

Host: Cursor cloud agent, not FlightSim. Synthetic only. No archive bodies.

`MEMORYBOX_ALLOW_DEV_DEFAULTS=1 python3 -m memorybox prove-trusted-identity-retrieval`

Results: `problems: []`. Review checks 51/51, including first-person promo, targeted neighbor SQL, fail-closed prepare, missing/mutated replay binding, and same-unit measurement that flags excluded “Happy birthday” while keeping retained “Love you”.

Not run: `prepare-trusted-email-review --flightsim`.

## FlightSim / human / model

- Code-complete (C1h unit checks): **YES**
- FlightSim-prepared: **NO** (new `REVIEW_*` after pull; keep prior folders)
- Human-approved hash: **NO**
- Inference: **NONE**
- Chunk experiment: **NOT STARTED**

## How to regenerate (FlightSim)

Refuse dirty/merge/rebase without changing them:

```
cd C:\memorybox
git rev-parse --abbrev-ref HEAD
git status
```

If `git status` is not clean, or MERGE_HEAD / rebase is in progress: **stop and inspect**. Do not commit, abort, or prepare.

Only if the tree is clean and on `cursor/p2-i11a-trusted-identity-retrieve-49da`:

```
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
git rev-parse HEAD
tools\flightsim-prepare-trusted-email-review.cmd
```

Inspect the **new** MODEL_PASTE for `===== REPLAY BINDING =====` and PREPARATION_REPORT `sanitation_token_compare_unit` / `sanitation_body_tokens_*` / `sanitation_paste_tokens_*` / `sanitation_human_evidence_loss_*` lines. Keep prior REVIEW_* folders. Do not git-add. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

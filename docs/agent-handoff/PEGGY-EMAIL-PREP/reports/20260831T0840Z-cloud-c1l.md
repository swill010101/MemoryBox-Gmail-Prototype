MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1l
run_id: 20260831T0840Z-cloud-c1l
code_commit: 45be4961370353069502b2c8a1fd092fe46ed6d5
stage: preparation-only
scope: membership-thanks-attribution-and-recomputed-replay-budget
status: READY_FOR_REVIEW

## Lightweight PRD (C1l)

Problem: Advisories from C1h–C1k left two Gemma blockers open. (1) “Thank you for being a member” (and similar) can become Peggy speech via greeting/wrapper short-circuit. (2) Replay oversize trusted mutable `prompt_tokens` / `usable_input_tokens` on SOURCE_MAP instead of the frozen paste + bound `num_ctx`.

Success: membership thank-you templates are not personal unless a genuine wrapper remains; replay recomputes prompt size and usable room from paste + binding.

In: authorship classify + residual promo + `plan_gemma_replay` budget; synthetic proves.
Out: chunking, Gemma/Sol execution, merge, FlightSim prepare (Tom). Does not overwrite C1k/C1j/C1i reports.

C1k neighbor canonicalization is unchanged in this pass.

## What changed (code at bound SHA)

1. **Membership thanks.** `_MEMBERSHIP_THANKS` / `_GENUINE_PERSONAL_WRAPPER`. Greeting short-circuit and trusted-From fallback do not treat “thank you for being a member/customer/subscriber” as speech. `I miss you` / `picked this` still personal.
2. **Replay budget.** `replay_usable_input_tokens(num_ctx)` = num_ctx − gen room − safety. Replay measures the actual paste; refuses sidecar usable larger than recomputed, sidecar prompt smaller than recomputed, or recomputed prompt above usable. Does not truncate.

## Tests actually run

`MEMORYBOX_ALLOW_DEV_DEFAULTS=1 python3 -m memorybox prove-trusted-identity-retrieval` → `problems: []` at `45be4961370353069502b2c8a1fd092fe46ed6d5`.

New: `review_membership_thanks_is_not_personal_speech`, `review_replay_plan_refuses_sidecar_prompt_understatement`, `review_replay_plan_refuses_sidecar_usable_more_permissive`. Oversize now uses a large paste vs recomputed usable from num_ctx 8192.

Not run: FlightSim prepare. No Gemma. No Sol. No chunks.

## FlightSim / human / model

- FlightSim-prepared after C1l: **NO**
- Human-approved hash: **NO**
- Year-fair Gemma+Sol FEV2: **NO** (`3cf95fa4` is not Phase 2)
- Semantic chunking: **NO**
- Inference: **NONE**

## How to regenerate (FlightSim)

```
cd C:\memorybox
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
python -m memorybox prepare-trusted-email-review --person "Peggy George" --flightsim
```

Keep prior REVIEW_*. Do not git-add. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

MB_REVIEW_READY
task: PEGGY-EMAIL-PREP
handoff: R1
correction_pass: C1c
run_id: 20260830T1310Z-cloud-c1c
code_commit: 793022f9be311c4fa3a7a588b9e0cb37f765057b
stage: preparation-only
status: READY_FOR_REVIEW

## Why C1c (does not overwrite C1 / C1b)

Field finding on PR #77 after Tom's FlightSim deploy: the regenerated paste still treated a long e-card template plus automation footer as Peggy's personal greeting, and kept the quoted template in another person's thank-you. Relabeling is not exclusion. This pass completes that outstanding correction only. Other open C1b findings stay open.

## What changed (C1c)

1. **Attribution.** Text before a strong automation marker is personal only if it is a short greeting (`Hi`/`Thanks`/…, ≤160 chars). A long service template plus footer is `service_generated`, even with a trusted From. Synthetic HTML and plain-text fixtures. No Hallmark-word blacklist.
2. **Packet admission.** A packet stays only when some turn has independently attributable Peggy personal speech. Another person's thank-you does not qualify. Unresolved trusted-From no longer admits a packet.
3. **Quoted service.** Quoted/forwarded service templates are omitted (`omitted_service_notice`) and recorded. Genuine short replies and unique non-service quotes stay. Rendered-paste checks, not labels only. `personal_plus_service` now also prints `service_body`.
4. **Counts.** Preparation report/source map record excluded packets and omitted service quotes. Originals are not deleted; retrieve/Gallery unchanged.
5. **Prepare wrapper.** Sync refuses to finish a merge or prepare unless `HEAD` is `cursor/p2-i11a-trusted-identity-retrieve-49da`.

## Tests actually run

Host: Cursor cloud agent, not FlightSim. Synthetic unit checks only. No archive bodies. No live Gemma/Sol.

Results: `problems: []`. Review checks 31/31 PASS.

Not run: `prepare-trusted-email-review --flightsim`.

## FlightSim / human / model

- Code-complete (C1c unit checks): **YES**
- FlightSim-prepared: **NO** (new `REVIEW_*` after pull; keep the prior failed artifact; gitignored)
- Human-approved hash: **NO**
- Inference: **NONE**
- Model-evaluated: **NO**

Other C1b items (full request-hash binding, reply-id expansion, SOURCE_MAP edge dump, timezone sort) remain open.

## How to regenerate the local packet (FlightSim)

If git is waiting for a commit message: `Esc`, `:cq`, Enter. Then:

```
cd C:\memorybox
set GIT_MERGE_AUTOEDIT=no
set GIT_EDITOR=true
git -c core.editor=true commit --no-edit -m "sync(flightsim): finish merge for trusted email review prepare"
git fetch origin cursor/p2-i11a-trusted-identity-retrieve-49da
git -c core.editor=true pull --rebase --no-edit origin cursor/p2-i11a-trusted-identity-retrieve-49da
git rev-parse HEAD
tools\flightsim-prepare-trusted-email-review.cmd
```

Expect `793022f9be311c4fa3a7a588b9e0cb37f765057b` or the following report commit. Inspect the **new** `MODEL_PASTE.txt`. Keep the previous `REVIEW_*` folder. Confirm the e-card notice and its quoted copy are absent. Do not git-add. Do not run Gemma.

WAITING FOR TOM — NO MODEL EXECUTION AUTHORIZED.

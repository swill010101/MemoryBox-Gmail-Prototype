# Derived / private runtime data (never sync via git)
.memorybox_ask_history.json
immich-activity.jsonl
.memorybox_sms_attach_index.json
working/
attachments/
database/
indexes/
exports/
backup/
logs/

# Private C1T benchmark inputs/results (inventory, email chunks, model outputs).
docs/test-output/c1t-benchmark/**
docs/test-output/trusted-email-review/**

# Archive: track Takeout originals + checksums only (zips via Git LFS).
# Ignore other archive contents if added later.
archive/**
!archive/google-takeout-original/
!archive/google-takeout-original/**
!archive/google-takout-original/
!archive/google-takout-original/**
!archive/checksums/
!archive/checksums/**
# Credentials / secrets
*.env
config/immich.env
config/secrets.json
config/local.json
config/historian_capture_gmail_credentials.json
config/historian_capture_gmail_token.json
config/marvin_capture.json

# Historian Capture / Guided Capture local runtime (never commit)
.memorybox_hc_fake_mail/
.memorybox_gc_fake_mail/
.memorybox_hc_mail/
.memorybox_capture_fake/
memorybox_artifact_media/
# Python
.venv/
__pycache__/
*.pyc
*.pyo
# Node / frontend build
node_modules/
application/**/dist/
application/**/build/
# Editors
.vscode/
.idea/
# Operating system
Thumbs.db
.DS_Store
Desktop.ini
MemoryBox_P1_and_Mockup_Screens_PNG.zip
ux-png-pack/
mockups/MemoryBox_P1_and_Mockup_Screens_PNG.zip

# Isolated synthetic I13 database validation (never runtime data).
/.i13-annotation-test-pg/
/.i13-annotation-test-pg.log
